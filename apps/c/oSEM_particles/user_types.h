/*
 * user_types.h -- OPS Particles oSEM on OPS Particles
 *
 * No user-defined types are needed here. The file exists because the OPS
 * translator looks for it by convention (it is optional -- __main__.py uses
 * safeFind -- but every comparable app in this tree ships one).
 */
#ifndef _USER_TYPES_H_
#define _USER_TYPES_H_
#endif
